import { withBemMod, ModBody } from '@bem-react/core';
/*
import { ITileDataProps } from '../index';
import TileData from '../TileData';
import Picture from '../../Picture';
import * as React from 'react';
import Button from '../../Button';

export interface ITileDataButtons extends ITileDataProps {
  dataType: 'buttons';
}

const DataButton: ModBody<ITileDataButtons> = (TileData, { className, text }) => {
  return <Button className={className} />;
};

export const TileDataAudio = withBemMod<ITileDataAudio>('Data', { dataType: 'audio' }, DataAudio);
*/
